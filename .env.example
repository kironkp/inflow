DEBUG=True
SECRET_KEY=replace-with-a-long-random-string
DB_NAME=findit
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Serper.dev / Google Images (image fallback when no photo uploaded)
SERPER_API_KEY=

# OpenAI — Whisper (dictation) + GPT-4o-mini (bulk-item parsing)
OPENAI_API_KEY=
